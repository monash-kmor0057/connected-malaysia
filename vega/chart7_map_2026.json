{
  "$schema": "https://vega.github.io/schema/vega-lite/v5.json",
  "width": 320,
  "height": 250,
  "title": { "text": "January 2026", "anchor": "start", "offset": 0, "fontSize": 18, "fontWeight": 600 },
  "projection": { "type": "mercator" },
  "layer": [
    {
      "data": {
        "url": "data/geo/basemap_view.json",
        "format": { "type": "geojson" }
      },
      "mark": {
        "type": "geoshape",
        "fill": "#eef2f7",
        "stroke": "#94a3b8",
        "strokeWidth": 0.55
      }
    },
    {
      "data": {
        "url": "data/geo/gps_mobile_tiles_2026.json",
        "format": { "type": "json", "property": "features" }
      },
      "transform": [
        { "calculate": "datum.properties.rate_mean", "as": "rate_mean" },
        { "calculate": "datum.properties.tile_count", "as": "tile_count" },
        { "filter": "isValid(datum.rate_mean) && isFinite(datum.rate_mean)" }
      ],
      "mark": {
        "type": "geoshape",
        "stroke": "#ffffff",
        "strokeWidth": 0.15,
        "opacity": 0.9
      },
      "encoding": {
        "color": {
          "field": "rate_mean",
          "type": "quantitative",
          "scale": {
            "domain": [3000, 130000],
            "clamp": true,
            "interpolate": "lab",
            "range": [
              "#3288bd",
              "#66b2d3",
              "#9ecae1",
              "#cfe8f3",
              "#fdd0bc",
              "#fa9a6d",
              "#ef6548",
              "#f03b20"
            ]
          },
          "legend": null
        },
        "tooltip": [
          { "field": "rate_mean", "title": "Tile mean download (kbps)", "format": ",.0f" },
          { "field": "tile_count", "title": "Tests in tile", "format": ",.0f" }
        ]
      }
    }
  ],
  "padding": { "left": 0, "top": 0, "right": 0, "bottom": 0 },
  "config": {
    "view": { "stroke": null, "fill": "#ffffff", "clip": true },
    "background": "#ffffff",
    "legend": { "symbolStrokeWidth": 0 }
  }
}
